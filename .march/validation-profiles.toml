# BioMCP validation profiles read by March at worktree time.
# Reserved names: preflight, baseline, focused, full-blocking, full-contracts.

# observed pending on beelink (2026-04-13)
[profile.preflight]
command = "cargo check --all-targets"

# observed pending on beelink (2026-04-13)
[profile.baseline]
command = "cargo check --all-targets"

# observed pending on beelink (2026-04-13)
[profile.focused]
command = "cargo test --lib && cargo clippy --lib --tests -- -D warnings"

# observed pending on beelink (2026-04-13)
[profile.full-blocking]
command = "make check && make spec-pr"

# observed pending on beelink (2026-04-13)
[profile.full-contracts]
command = "make check && make spec-pr && make test-contracts"
